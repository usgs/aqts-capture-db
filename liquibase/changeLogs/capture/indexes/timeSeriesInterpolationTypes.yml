databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    - runningAs:
        username: ${AQTS_SCHEMA_OWNER_USERNAME}

  - changeSet:
      author: drsteini
      id: "create.index.${AQTS_SCHEMA_NAME}.time_series_interpolation_types_json_data_id"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              schemaName: ${AQTS_SCHEMA_NAME}
              indexName: time_series_interpolation_types_json_data_id
      changes:
        - sql: create index ${AQTS_SCHEMA_NAME}.time_series_interpolation_types_json_data_id on ${AQTS_SCHEMA_NAME}.time_series_interpolation_types (json_data_id);
        - rollback: drop index if exists ${AQTS_SCHEMA_NAME}.time_series_interpolation_types_json_data_id;
